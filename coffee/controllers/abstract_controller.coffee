class AbstractController
    constructor: () ->

    onDblClick: (x, y) ->

    onMouseDown: (x, y) ->

    onMouseUp: (x, y) ->

    onMouseMove: (x, y) ->

    onMouseLeave: (x, y) ->

    toString: () ->
        @constructor.name